import React, { use } from "react";
import { useState, useEffect } from "react";

function Camera({port}) {
  const [cameraSettings, setCameraSettings] = useState({

  })

  useEffect(
    ()=>{
      setCameraSettings({
        
      })
    }
  )

  const handleInputChange = () => {

  };

  return (
    <div>
      
    </div>
  );
}